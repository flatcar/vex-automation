# Binaries
/bin/
/dist/

# Test / coverage artifacts
*.out
coverage.txt
coverage.html

# Editor / OS
.DS_Store
.idea/
.vscode/

# Go build cache locally (rare)
/vendor/

# Local scratch data for manually trying out the CLI (downloaded SBOMs, GLSA
# corpus mirrors, generated VEX output) — never committed.
/.local/
